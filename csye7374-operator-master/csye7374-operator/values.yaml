# Default values for go-k8s.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1

image:
  repository: akmurgod/k8s-operator
  tag: 0.1.2
  pullPolicy: Always

service:
  name: csye7374-operator
  type: ClusterIP
  protocol: TCP
